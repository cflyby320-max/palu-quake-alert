{
  "name": "palu-quake-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Instagram content studio for Palu Earthquake Alerts — renders branded shakemap cards + Indonesian captions. Deliberately SEPARATE from the zero-dependency watcher in ../src; a failure here must never affect alert delivery.",
  "scripts": {
    "demo": "node studio.js --demo"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
